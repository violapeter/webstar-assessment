# UI React

Business-specific React components for the WebStar application. Built on top of the UI Library, these components provide complete, feature-rich implementations for character management, authentication, and user interface elements.

## Overview

UI React contains higher-level React components that combine UI Library primitives with business logic. These components are specifically designed for the application's character management system.
